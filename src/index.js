import React from 'react';
import ReactDOM from 'react-dom';
import App from "../src/components/App.js"; // Import your root component (usually named App)

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
